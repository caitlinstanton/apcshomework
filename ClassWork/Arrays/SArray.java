public class SArray {
    //variableType of the list should be object to be general
    private int[] data;

    public SArray (){
	//start array at size 10

    }

    /*
    //add to the end
    add(int i);
    //add() at location index and shift everything else down
    add(int index, int i);
    //get
    get(int i);
    get(int index, int i);
    size();
    remove(int index);
    */
